const summary = document.querySelector(".changer");
function promptUser() {
    var password = prompt("Please enter your password:");
    if(password == "secret"){
        summary.textContent = prompt("modify it")
    }else{
        alert("Incorrect password.");
    }
}
function promptUser2() {
    var password = prompt("Please enter your password:");
    if(password == "secret"){
        summary.textContent = prompt("modify it")
    }else{
        alert("Incorrect password.");
    }
}
function promptUser3() {
    var password = prompt("Please enter your password:");
    if(password == "secret"){
        summary.textContent = prompt("modify it")
    }else{
        alert("Incorrect password.");
    }
}
function promptUser4() {
    var password = prompt("Please enter your password:");
    if(password == "secret"){
        summary.textContent = prompt("modify it")
    }else{
        alert("Incorrect password.");
    }
}
function promptUser5() {
    var password = prompt("Please enter your password:");
    if(password == "secret"){
        summary.textContent = prompt("modify it")
    }else{
        alert("Incorrect password.");
    }
}